# X-Agent 产品介绍 🤖

> **你的个人 AI 智能体平台** — 不止于对话，而是真正理解你、记住你、帮你做事的智能伙伴。

---

## 🎯 产品定位

X-Agent 是一个面向个人和企业的 **AI 智能体平台**，解决传统 AI 对话工具的三大痛点：

- ❌ **失忆问题** → ✅ 永久记忆，跨会话上下文自动召回
- ❌ **单一角色** → ✅ 多智能体协作，各司其职
- ❌ **只会聊天** → ✅ 工具扩展，真正动手做事

---

## 📸 产品截图

### Web 对话界面

![Web 对话界面](babackend/src/docs/images/screenshot/chat-ui.png)

> 支持 Markdown 实时渲染、代码高亮、流式输出，对话体验流畅自然。

### 多智能体管理

![多智能体管理](backend/src/backend/src/docs/images/screenshot/agent-management.png)

> 每个 Agent 拥有独立人设、独立记忆、独立工具配置，灵活切换。

### Trace 可观测面板

![Trace 可观测面板](backend/src/docs/images/screenshottrace-panel.png)

> 全链路可视化追踪，每个请求的调用链、耗时、LLM 详情一目了然。

### 定时任务管理

![定时任务管理](backend/src/docs/images/screenshotcron-dashboard.png)

> Cron 调度引擎，支持标准 Cron 表达式和自然语言间隔，任务全生命周期管理。

### CLI 命令行界面

![CLI 命令行界面](backend/src/docs/images/screenshotcli-interface.png)

> 极客友好的命令行体验，支持交互式对话、Agent 管理、定时任务等全部功能。

---

## ✨ 核心功能

### 🧠 1. 永久记忆系统

不像普通 ChatGPT 每次对话都"从零开始"，X-Agent 自动将重要信息存入记忆库：

- **自动摘要** — 对话结束后自动提炼关键信息
- **语义检索** — 下次对话时，相关上下文自动召回
- **向量化存储** — 基于语义相似度精准匹配历史记忆

```
你："上周说的那个方案怎么样了？"
AI："您指的是3月1日讨论的微服务拆分方案吗？当时我们决定..."
```

---

### 🤖 2. 多智能体协作

一个平台，多个专业 Agent，各司其职：

| Agent 类型 | 专业领域 | 典型能力 |
|-----------|---------|---------|
| 代码审查助手 | 代码质量 | 发现 Bug、安全漏洞、最佳实践建议 |
| 写作助手 | 内容创作 | 文章撰写、润色、翻译 |
| 运维助手 | 自动化运维 | 定时任务、系统监控、告警处理 |
| 私人助理 | 日程管理 | 邮件回复、会议提醒、任务跟踪 |

```bash
# 创建专业代码审查助手
x-agent agent create --name "CodeReviewer" \
  --persona "严格把控代码质量，专注发现潜在bug和安全漏洞"
```

---

### 🔧 3. Skills 工具生态

通过 Skills 给 AI 添加真实能力，热插拔扩展：

| Skill | 能力描述 | 典型场景 |
|-------|---------|---------|
| `auto-cr` | 自动代码评审、分析评论、触发修复 | 提交 PR 后自动审查 |
| `send-email` | 发送事务邮件、通知、批量邮件 | 定时发送报告 |
| `task-analysis` | 智能任务分析，R2C/T2C/D2C 全流程 | 需求到代码一键生成 |
| `find-skills` | 发现和安装新技能 | 扩展 Agent 能力 |

---

### ⏰ 4. 企业级定时任务引擎

**业界首创 CLI + Agent 双模调用**：

```bash
# 创建每天凌晨 2 点的备份任务
x-agent cron create -n "每日备份" \
    -s "0 2 * * *" \
    -f "workspace:backup.py:run" -y

# 创建每 30 分钟执行的健康检查
x-agent cron create -n "健康检查" -s "30m" \
    -f "workspace:health_check.py:check" -y

# 查看任务列表
x-agent cron list

# 查看执行历史
x-agent cron history
```

**支持的调度格式：**
- **Cron 表达式**：`0 2 * * *`（每天凌晨 2 点）、`30 8 * * 1-5`（工作日早上 8:30）
- **自然语言间隔**：`30m`（30 分钟）、`1h`（1 小时）、`2d`（2 天）

---

### 🌐 5. 多端统一交互

**Web 界面** — 最直观的体验
```bash
./start.sh  # 一键启动，自动打开浏览器
```

**CLI 命令行** — 极客的最爱
```bash
x-agent chat                      # 交互式对话
x-agent chat "写个 Python 爬虫"     # 单次对话
x-agent status                    # 查看系统状态
```

**IM 通道** — 在熟悉的平台直接使用
- 📱 飞书（Feishu）
- 💬 钉钉（DingTalk）
- 🏢 企业微信（WeChat Work）
- 🌐 Telegram

---

### 🔍 6. 全链路可观测性

每个请求都有唯一 Trace ID，在 Web 界面的 Dev Tools 中：

- 🔍 **可视化调用链** — 完整展示每个节点的执行流程
- ⏱️ **性能分析** — 每个节点的耗时和状态
- 📝 **LLM 详情** — Prompt、Response、Token 消耗全记录

---

## 🏗️ 技术架构

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         用户交互层（五端统一）                           │
├───────────┬───────────┬──────────────┬──────────────────────────────────┤
│  Web 界面  │ CLI 命令行 │ WebSocket    │      IM 通道（飞书/钉钉/企微）     │
│ (React+TS) │ (Python)  │  实时通信    │      (Channel Adapter)           │
└─────┬─────┴─────┬─────┴──────┬───────┴──────────────────────────────────┘
      └───────────┴────────────┘
                 │
┌────────────────▼────────────────────────────────────────────────────────┐
│                    Gateway 网关层（智能路由）                            │
│         统一入口、负载均衡、会话管理、Agent 调度、消息协议转换            │
└─────────────────────────────────────────────────────────────────────────┘
                              │
        ┌─────────────────────┼─────────────────────┐
        │                     │                     │
┌───────▼───────┐    ┌────────▼────────┐   ┌───────▼───────┐
│   Agent 核心   │    │    记忆系统      │   │   工具系统     │
│  - 多轮对话    │    │  - 向量化存储    │   │  - Skills     │
│  - 上下文压缩  │    │  - 语义检索      │   │  - 代码执行    │
│  - 人设管理    │    │  - 自动摘要      │   │  - 外部 API   │
└───────────────┘    └─────────────────┘   └───────────────┘
```

---

## 🚀 快速开始

### 环境要求

- Python 3.10+
- Node.js 18+
- 有效的 LLM API Key（支持 OpenAI、Claude、通义千问等）

### 一键启动

```bash
# 克隆项目
git clone https://github.com/your-org/x-agent.git
cd x-agent

# 配置环境
cp backend/x-agent.yaml.example backend/x-agent.yaml
# 编辑 x-agent.yaml，填入你的 API Key

# 一键启动（Web + Backend）
./start.sh
```

### CLI 安装

```bash
cd cli
pip install -e .

# 验证安装
x-agent --version
x-agent status
```

---

## 📋 功能对比

| 功能 | 普通 AI 对话 | X-Agent |
|------|------------|---------|
| 跨会话记忆 | ❌ | ✅ 永久记忆 |
| 多 Agent 管理 | ❌ | ✅ 无限创建 |
| 工具扩展 | ❌ | ✅ Skills 生态 |
| 定时任务 | ❌ | ✅ Cron 引擎 |
| IM 平台接入 | ❌ | ✅ 5+ 平台 |
| 全链路追踪 | ❌ | ✅ Trace 面板 |
| CLI 支持 | ❌ | ✅ 完整 CLI |
| 私有化部署 | ❌ | ✅ 完全自托管 |

---

## 📞 联系我们

- 📖 **文档**：[查看完整文档](README.md)
- 🐛 **问题反馈**：提交 Issue
- 💬 **社区讨论**：加入我们的讨论群
